# Claude Code instructions

`AGENTS.md` is the shared source of truth for Claude Code and Codex, including
the project's `## This repo` rules. `ORCHESTRATION.md` is the read-on-demand
playbook linked from that baseline.

@AGENTS.md

<!-- Add only genuinely Claude-specific rules here. Shared project rules belong
     under `## This repo` in AGENTS.md so both engines receive them. -->
